<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Offre Exclusive - {{companyName}}</title>
    <style>
        /* Styles de base */
        body {
            margin: 0;
            padding: 0;
            background-color: #f0f4f7;
            font-family: 'Segoe UI', Arial, sans-serif;
            color: #555;
        }

        table {
            border-spacing: 0;
            width: 100%;
            max-width: 600px;
            margin: 30px auto;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        }

        img {
            display: block;
            max-width: 100%;
            height: auto;
        }

        a {
            text-decoration: none;
            color: #ffffff;
        }

        /* En-tête */
        .header {
            background: linear-gradient(135deg, {{companyPrimaryColor}} 0%, {{companySecondaryColor}} 100%);
            padding: 40px;
            text-align: center;
            color: #ffffff;
            border-bottom: 5px solid {{companyAccentColor}};
            position: relative;
        }

        .header h1 {
            font-size: 32px;
            font-weight: bold;
            margin: 0;
        }

        .header p {
            margin-top: 10px;
            font-size: 18px;
            color: #f8f8f8;
        }

        .header .badge {
            background-color: #ff6b6b;
            padding: 10px 20px;
            border-radius: 50px;
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 14px;
            font-weight: bold;
            text-transform: uppercase;
        }

        /* Bannière principale */
        .hero {
            background-image: url('{{bannerImageUrl}}');
            background-size: cover;
            background-position: center;
            padding: 60px;
            color: #ffffff;
            text-align: center;
            position: relative;
        }

        .hero h2 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #ffffff;
        }

        .hero p {
            font-size: 16px;
            line-height: 1.6;
            color: #f0f0f0;
        }

        .hero::before {
            content: '';
            background: rgba(0, 0, 0, 0.4);
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        /* Section d'offre spéciale */
        .offer {
            padding: 40px 30px;
            text-align: center;
            background-color: #f9f9f9;
        }

        .offer h3 {
            font-size: 24px;
            color: {{companyPrimaryColor}};
            margin-bottom: 20px;
        }

        .offer p {
            font-size: 16px;
            line-height: 1.6;
            color: #666;
            margin-bottom: 30px;
        }

        .offer .cta-button {
            display: inline-block;
            background-color: {{companyPrimaryColor}};
            color: #ffffff;
            padding: 15px 30px;
            font-size: 18px;
            font-weight: bold;
            border-radius: 50px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .offer .cta-button:hover {
            background-color: {{companyAccentColor}};
            transform: scale(1.05);
        }

        /* Grille d'articles */
        .grid {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            padding: 40px;
        }

        .grid-item {
            flex: 1 1 calc(33.333% - 20px);
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .grid-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .grid-item img {
            border-bottom: 2px solid {{companyAccentColor}};
        }

        .grid-item h4 {
            font-size: 20px;
            margin: 15px;
        }

        .grid-item p {
            font-size: 14px;
            color: #777;
            margin: 15px;
        }

        /* Section avec statistiques/bonus */
        .stats {
            padding: 50px 30px;
            background-color: {{companySecondaryColor}};
            color: #ffffff;
            text-align: center;
            border-radius: 0 0 12px 12px;
        }

        .stats-item {
            display: inline-block;
            margin: 0 20px;
        }

        .stats-item h4 {
            font-size: 36px;
            margin: 0;
        }

        .stats-item p {
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 5px;
            opacity: 0.8;
        }

        /* Pied de page */
        .footer {
            padding: 30px;
            text-align: center;
            background-color: #333333;
            color: #ffffff;
        }

        .footer p {
            font-size: 12px;
            color: #bbbbbb;
            margin: 0;
        }

        .footer a {
            color: #ffffff;
            text-decoration: underline;
        }

        /* Animation */
        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero-content,
        .offer,
        .stats-item,
        .grid-item {
            animation: fadeInUp 0.5s ease-in-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .grid-item {
                flex: 1 1 calc(50% - 20px);
            }

            .hero h2 {
                font-size: 24px;
            }

            .offer h3 {
                font-size: 20px;
            }
        }

        @media (max-width: 480px) {
            .grid-item {
                flex: 1 1 100%;
            }

            .header h1 {
                font-size: 24px;
            }

            .hero h2 {
                font-size: 20px;
            }

            .offer h3 {
                font-size: 18px;
            }

            .stats-item h4 {
                font-size: 30px;
            }

            .offer .cta-button {
                padding: 12px 20px;
                font-size: 16px;
            }
        }
    </style>
</head>

<body>
    <table>
        <!-- En-tête -->
        <tr>
            <td class="header">
                <h1>Offre Exclusive</h1>
                <p>Découvrez nos promotions limitées !</p>
                <span class="badge">Promotion</span>
            </td>
        </tr>

        <!-- Bannière Héro -->
        <tr>
            <td class="hero">
                <div class="hero-content">
                    <h2>{{offerTitle}}</h2>
                    <p>{{offerDescription}}</p>
                </div>
            </td>
        </tr>

        <!-- Section d'offre spéciale -->
        <tr>
            <td class="offer">
                <h3>Profitez de cette offre maintenant !</h3>
                <p>Cette promotion expire bientôt. Cliquez sur le bouton ci-dessous pour en bénéficier avant qu'il ne soit trop tard.</p>
                <a href="{{offerLink}}" class="cta-button">Obtenez l'Offre</a>
            </td>
        </tr>

        <!-- Grille de produits -->
        <tr>
            <td class="grid">
                <div class="grid-item">
                    <img src="{{productImageUrl1}}" alt="Produit 1">
                    <h4>Produit 1</h4>
                    <p>Description du produit 1 ici...</p>
                </div>
                <div class="grid-item">
                    <img src="{{productImageUrl2}}" alt="Produit 2">
                    <h4>Produit 2</h4>
                    <p>Description du produit 2 ici...</p>
                </div>
                <div class="grid-item">
                    <img src="{{productImageUrl3}}" alt="Produit 3">
                    <h4>Produit 3</h4>
                    <p>Description du produit 3 ici...</p>
                </div>
            </td>
        </tr>

        <!-- Statistiques et bonus -->
        <tr>
            <td class="stats">
                <div class="stats-item">
                    <h4>50%</h4>
                    <p>Réduction</p>
                </div>
                <div class="stats-item">
                    <h4>24H</h4>
                    <p>Temps Restant</p>
                </div>
                <div class="stats-item">
                    <h4>500+</h4>
                    <p>Produits Disponibles</p>
                </div>
            </td>
        </tr>

        <!-- Pied de page -->
        <tr>
            <td class="footer">
                <p>&copy; 2024 {{companyName}}. Tous droits réservés.</p>
                <p>Si vous ne souhaitez plus recevoir d'emails de notre part, <a href="{{unsubscribeLink}}">cliquez ici</a>.</p>
            </td>
        </tr>
    </table>
</body>

</html>
